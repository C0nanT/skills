# Agent standing context

Source of truth: [`CLAUDE.md`](./CLAUDE.md).

Before any other repo work, **Read `CLAUDE.md` in full** and follow it. Do not treat this file as a substitute, summary, or second copy.

Cursor: `@CLAUDE.md`

Edit `CLAUDE.md` only. Shared domain material (`CONTEXT.md`, `.agents/`, `docs/agents/`) is already harness-neutral — do not copy standing context into `.cursor/rules/` or another agent markdown file.
